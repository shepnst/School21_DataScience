# Intro to Python: Package management and virtual environment

Summary: Today we will help you acquire basic knowledge about how to manage libraries in
Python and work with virtual environments.<br>
virtual environment(installing packages), parcing(BeautifulSoup), requests, PyTest, Profiling(cProfile), pstats

